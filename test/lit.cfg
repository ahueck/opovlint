import os
import sys
import re
import platform
import subprocess

import lit.util
import lit.formats

config.name = "BasicTests"

execute_external = (True)
config.test_format = lit.formats.ShTest(execute_external)

config.suffixes = ['.cpp']
